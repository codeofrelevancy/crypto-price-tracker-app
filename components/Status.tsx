import React from "react";

interface Props {
  label: string;
  value: string;
}

export default function Status({ label, value }: Props) {
  return (
    <div>
      {/* TODO: crypto status code */}
    </div>
  );
}
